# SeferFlow - Core Dependencies

# Build system (required first)
setuptools>=45.0

# Audio processing and playback
soundfile>=0.12.1
sounddevice>=0.4.5
numpy>=1.21.0

# Text-to-Speech
edge-tts>=6.1.0

# Terminal UI and keyboard controls
rich>=13.0
readchar>=4.0

# Optional: Local offline TTS (install separately if needed)
# kokoro-onnx>=0.1.0

# Optional: Testing
# pytest>=7.0
# pytest-cov>=3.0

# Optional: Development
# black>=22.0
# flake8>=4.0
# mypy>=0.900

# System requirements (install via package manager, not pip):
# - pdftotext (poppler-utils on Linux/Mac)
# - aplay, paplay, or ffplay (for audio fallback)
